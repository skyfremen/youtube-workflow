name: Wacky Dramas Acceptance Recovery

on:
  push:
    branches: [main]
    paths: ['.github/workflows/wacky-dramas-acceptance-recovery.yml']
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  recover-existing:
    uses: ./.github/workflows/adhoc-story-private.yml
    with:
      content_id: wd-20260909T004900-unpaid-overtime-c84f2a
      recovery_only: true
      render_preview: true
    secrets: inherit
