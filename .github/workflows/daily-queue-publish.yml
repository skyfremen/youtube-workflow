name: Legacy Wacky Insights queue publisher disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Migration freeze guard
        run: |
          echo 'Legacy Wacky Insights 24-slot queue publishing is disabled during the Wacky Dramas migration.'
          echo 'This workflow has no YouTube credentials, upload step, schedule loop, or publication path.'
