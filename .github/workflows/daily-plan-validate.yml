name: Legacy Wacky Insights planner disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Migration freeze guard
        run: |
          echo 'Legacy Wacky Insights daily planning/validation is disabled during the Wacky Dramas migration.'
          echo 'Do not re-enable this workflow. A future Wacky Dramas daily system will be designed separately.'
